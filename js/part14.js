// do {
//     // statement
//     // increment/ decrement

// } while (condition);

let iterationValue = 1;
do {
    document.write(`<h1>${iterationValue}. I ❤️ Code Fusion</h1>`);
    iterationValue++;
}
while (iterationValue <1);
